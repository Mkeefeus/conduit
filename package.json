{
  "name": "conduit",
  "module": "build.js",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.4.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "destructure": "bun run destructure.js && bun run format",
    "build": "bun run build.js && bun run format",
    "format": "prettier --write ."
  },
  "dependencies": {
    "prompt": "^1.3.0"
  }
}
